datasource db {
  provider = "sqlite"
}

generator client {
  provider = "prisma-client-js"
}

model Portfolio {
  id           Int    @id @default(autoincrement())
  symbol       String
  addedPrice   Float
  currentPrice Float
}

model Watchlist {
  id           Int    @id @default(autoincrement())
  symbol       String
  addedPrice   Float
  currentPrice Float
}
